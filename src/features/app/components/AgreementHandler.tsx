// export const handlerAgreement = (status: boolean, popUp: boolean) => {
//     if (!status) {
//         setShowPopup(false);
//         return false;
//     }

//     if (location) {
//         const existing = JSON.parse(localStorage.getItem("marker") || "[]");

//         const updated = [...existing, location];

//         localStorage.setItem("marker", JSON.stringify(updated));
//     }

//     setShowPopup(false);
//     return true;
// };